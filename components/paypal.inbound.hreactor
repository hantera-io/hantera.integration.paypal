from {
  createPayment = (opts: {
    currencyCode: text
  }) =>
    messageActor (
      'payment'
      'new'
      'create'
      {
        currencyCode = opts.currencyCode
        privderKey = 'stripe'
      }
    )
}